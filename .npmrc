# Peer ranges use `^0.1.1-rc.2 || ^0.1.2-rc.1` so the plugin loads under
# either host; npm cannot dedupe across the prerelease boundary, so local
# installs use legacy-peer-deps to mirror the host's mixed resolution.
legacy-peer-deps=true
